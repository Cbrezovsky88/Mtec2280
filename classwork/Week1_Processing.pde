//creae canvase with x,y formate
size(500,500);
//Add collor to ccanvas with either blsck to white or RGB
background(9,69,90);
//Fill the Space with color rgb.
fill(619,0,22);
//the change the outline color
stroke(0,255,0);
//to disable the stroke entirely use noStroke();
//Create a rectangle use=ing x,y and the first two parameters and the next two parameters are width and length.
rect(50,40,100,200);
//Works like rect but its the center of the circle thats being placed. Parameter, x,y,widthi,height.
//Fourth is Alpha which is transperency.
fill(0,69,69, 127);
ellipse(400,300,50,60);
